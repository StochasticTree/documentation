---
title: "Design Principles and Proposals"
---

::: {.callout-note}
## Release-candidate documentation

This page documents the **0.5.0 architecture overhaul** and only appears on the
release-candidate site (`stochtree.ai/v/rc-0.5.0/`). Use the **Version** menu in
the navbar to switch back to the latest released docs.
:::

*Placeholder — replace with the design principles and proposals for the 0.5.0
overhaul.* Add further `.qmd`/`.md` pages on this `rc-0.5.0` branch and wire them
into the nav in `_quarto.yml`; they will build into the versioned sub-site
without touching the latest site.
